name: Deploy React App to Ubuntu Server

on:
  push:
    branches: ["react-web"]

jobs:
  deploy:
    # Use the exact labels from your runner configuration
    runs-on: [self-hosted, Linux, X64] # These are the default labels
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build
        env:
          REACT_APP_API_BASE_URL: ${{ secrets.REACT_APP_API_BASE_URL }}

      - name: Create production server
        run: |
          # Create a simple Node.js server for the React app
          echo 'const express = require("express");
          const path = require("path");
          const app = express();
          const port = process.env.PORT || 3000;

          app.use(express.static(path.join(__dirname, "build")));

          app.get("*", (req, res) => {
            res.sendFile(path.join(__dirname, "build", "index.html"));
          });

          app.listen(port, () => {
            console.log(`React app server running on port ${port}`);
          });' > server.js

          echo '{
            "name": "react-app-server",
            "version": "1.0.0",
            "description": "Simple server for React app",
            "main": "server.js",
            "scripts": {
              "start": "node server.js"
            },
            "dependencies": {
              "express": "^4.18.2"
            }
          }' > package-production.json

      - name: Install production dependencies
        run: npm install express --save

      - name: Setup systemd service for React app
        run: |
          echo "[Unit]
          Description=React App Server
          After=network.target

          [Service]
          ExecStart=/usr/bin/node /home/ferytell/react-app/server.js
          WorkingDirectory=/home/ferytell/react-app
          User=ferytell
          Group=ferytell
          Restart=always
          Environment=NODE_ENV=production
          Environment=PORT=3000

          [Install]
          WantedBy=multi-user.target" | sudo tee /etc/systemd/system/react-app.service

      - name: Deploy React app files
        run: |
          # Create target directory
          mkdir -p /home/ferytell/react-app

          # Copy built files and server
          cp -r build/* /home/ferytell/react-app/
          cp server.js /home/ferytell/react-app/
          cp package-production.json /home/ferytell/react-app/package.json

      - name: Install production deps and restart service
        run: |
          cd /home/ferytell/react-app
          npm install
          sudo systemctl daemon-reload
          sudo systemctl enable react-app
          sudo systemctl restart react-app
          sudo systemctl status react-app --no-pager -l

#jekyll-gh-pages.yml
# name: Deploy React App to GitHub Pages

# on:
#   push:
#     branches: ["react-web"] # default branch

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20" # or your preferred version

#       - name: Install dependencies
#         run: npm install

#       - name: Build
#         run: npm run build

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: "./build" # This is the default build folder for Create React App

#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
